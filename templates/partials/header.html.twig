
<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{path('app_home')}}">SimImmo</a>

		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active" href="{{path('app_home')}}">Accueil

					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link active" href="{{path('app_annonces')}}">Annonces</a>


				</li>


			</ul>
			<ul class="navbar-nav ml-auto" style="margin-right:5%">
				{% if app.user %}

					<li class="nav-item dropdown">
						<a href="" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownlink">
							<img src="{{app.user.picture}}" class="avatar avatar-mini img-fluid rounded-circle " alt="avatar de {{app.user.fistName}}">
							{{app.user.lastName}}
						</a>
						<div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownlink">
							<a href="{{path('app_annonce_ad')}}" class="dropdown-item">Ajouter une annonce</a>
							<div class="dropdown-divider"></div>
							<a href="{{path('app_account')}}" class="dropdown-item">Mon compte</a>
							<a href="{{path('app_reservations')}}" class="dropdown-item">Mes reservations</a>


							<a href="{{path('app_profile')}}" class="dropdown-item">Modifier mon profil</a>
							<div class="dropdown-divider"></div>

							<a href="{{path('app_logout')}}" class="dropdown-item">Deconnexion</a>

						</div>
					</li>

				{% else %}
					<li class-"nav-item">
						<a href="{{path('app_register')}}" class="nav-link active">Inscription</a>


					</li>


					<li class-"nav-item">
						<a href="{{path('app_login')}}" class="nav-link active">Connexion</a>

					</li>


				{% endif %}

			</ul>
		</div>
	</div>
</nav>

